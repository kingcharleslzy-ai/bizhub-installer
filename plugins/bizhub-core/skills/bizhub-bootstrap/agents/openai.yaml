interface:
  display_name: "BizHub Bootstrap"
  short_description: "Build a safe, read-only BizHub setup plan."
  default_prompt: "Use $bizhub-bootstrap to inspect my Agent host and build a read-only BizHub setup plan."
policy:
  allow_implicit_invocation: false
